<%= link_to "Back", caravan_path, class: "btn btn-primary back-btn" %>
<%= simple_form_for(@caravan) do |f| %>
  <%= f.submit "Submit picture", class: "btn btn-primary" %>
  <%= f.input :photos, as: :file, input_html: { multiple: true } %>
<% end %>
