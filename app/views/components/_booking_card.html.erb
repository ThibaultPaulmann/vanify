<div id="booking-card">
  <h2 class="price">€ <%= @caravan.price %> / day </h2>
  <%= simple_form_for [@caravan, @booking] do |f| %>
    <%= f.input :start_date %>
    <%= f.input :end_date %>
    <%= f.submit %>
  <% end %>
</div>
